============
Installation
============

At the command line::

    $ easy_install django-dbcheck

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dbcheck
    $ pip install django-dbcheck
